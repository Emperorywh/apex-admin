"""密码与 Token 安全基元 — SPEC 12.1 / 12.2 / 23.2.

本包提供 G2 身份模块所需的底层安全原语，不包含登录、刷新或会话用例
（后者由 TASK-013/014 实现）。所有原语不依赖 FastAPI、ORM 或具体存储 SDK，
可独立进行单元测试。

子模块:
  - password: Argon2id 哈希服务（固定参数、随机盐、rehash 判定、密码长度策略）、
              防枚举虚拟哈希常量。
  - token:    CSPRNG Token 生成（>=256 bit 熵）。
  - digest:   HMAC-SHA-256 双密钥摘要服务（启动校验缺失/相同/长度不足即失败）。
"""
