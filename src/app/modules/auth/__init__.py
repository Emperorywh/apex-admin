"""认证模块 — 登录、会话、退出、Token 刷新与登录安全（SPEC 12.1~12.4 / 18.1）.

本模块实现 G2 的核心认证能力:
  - 账号密码登录（防枚举虚拟哈希、失败双维度限制、rehash 升级、no-store）。
  - 服务端会话持久化（设备/IP/UA/创建时间/最近活动时间）。
  - 会话过期规则（空闲 30 分钟、绝对 12 小时、最近活动时间 5 分钟条件更新）。
  - 认证依赖（每请求查库校验用户启用、会话有效、Token 有效、空闲/绝对过期）。
  - Refresh Token 轮换与重放检测（行锁、并发单成功、Family 吊销）。
  - ``__Host-apex_refresh`` Cookie 安全属性与 Origin 白名单校验。
  - 会话查看与退出（当前/其他）。
  - 事务内事件处理器（USER.DISABLED / USER.PASSWORD_RESET_BY_ADMIN 吊销会话）。
  - 登录日志接线（成功/失败/原因分类/退出/刷新异常）。

不包含:
  - RBAC 权限校验（TASK-015/016）。
  - 管理员强制下线 API（TASK-016）。
"""
