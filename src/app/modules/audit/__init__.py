"""审计与登录日志模块 — SPEC 18.1 / 18.2 / 5.7 / 8.3.

此模块提供:
  - 操作审计表（``audit_logs``）与登录日志表（``login_logs``）。
  - ``AuditPort``（同事务提交）— 成功操作审计与业务事务共同提交。
  - ``LoginLogPort`` — 登录日志记录。
  - ``SecurityLogPort`` — 失败操作独立安全日志渠道（不参与业务事务）。
  - 变更差异字段白名单机制（禁止反射式全字段序列化）。
  - 操作者/目标显示名按操作发生时快照保存。
  - 审计日志不可变约束（仅 INSERT，无 UPDATE/DELETE 应用层路径）。

本任务只建模块与 Port，业务接线由后续任务完成。
"""
