# 通用后台管理系统模板 需求文档（PRD）

## 文档说明

### 目的
本文档定义一套**通用后台管理系统前端模板**的功能、界面与非功能需求，作为产品规划、技术选型、UI 设计、开发实现与验收测试的统一依据。文档聚焦"任何后台管理系统都需要的共性能力"，不绑定具体业务领域，可作为脚手架/模板工程的基线规格。

### 适用范围
- 面向企业内部管理后台、运营平台、SaaS 控制台等 B 端中后台场景
- 作为通用脚手架的 baseline，具体业务模块（订单、商品等）在此基础上扩展

### 术语表
| 术语 | 含义 |
| --- | --- |
| RBAC | Role-Based Access Control，基于角色的访问控制 |
| 菜单/目录/按钮 | 权限资源的三种类型：目录为分组容器，菜单对应可访问页面，按钮对应页面内操作 |
| 权限标识 | 形如 `user:create` 的权限码，用于精细化鉴权 |
| 权限路由 | 基于前端路由注册表和当前用户权限过滤出的可访问路由 |
| routeKey/componentKey | 后端菜单数据与前端路由注册表之间的稳定关联 key，不等同于组件文件路径 |
| Token | 登录凭证，用于维持登录态 |
| Design Token | 设计令牌，以变量形式统一定义颜色/字号/间距等设计原子 |
| API Client | 基于 axios 封装的统一请求客户端，负责请求拦截、响应拦截、错误归一化和认证失效处理 |

### 优先级定义
| 等级 | 含义 |
| --- | --- |
| **P0** | 核心必备，MVP 必须实现，缺失则模板不可用 |
| **P1** | 重要推荐，显著提升可用性与体验，强烈建议纳入 |
| **P2** | 进阶扩展，按需选配，不强制 |

---

## 1. 产品概述

### 1.1 产品定位
提供一套开箱即用的后台管理系统**前端底座**，预置认证授权、组织权限、菜单布局、主题国际化、工程化等基础设施，使业务团队聚焦业务功能开发，无需重复搭建框架。

### 1.2 目标用户
- **开发者**：基于模板二次开发的后端/前端工程师
- **管理员**：使用系统进行用户、角色、权限、菜单配置的超级管理员
- **普通用户**：被授予特定权限、使用业务功能的运营/管理人员

### 1.3 设计原则
1. **通用优先**：提供共性能力，业务无关，可被任意领域复用
2. **配置驱动**：菜单、权限、主题等尽量通过配置/数据驱动，减少硬编码
3. **开箱即用与可裁剪并重**：核心功能默认启用，扩展功能可按需关闭
4. **类型安全与规范统一**：全量 TypeScript，统一的编码与提交规范
5. **响应式与无障碍**：适配多终端，遵循可访问性基本要求
6. **架构优先**：当功能堆叠与模块边界冲突时，优先重构边界而不是追加临时逻辑
7. **显式状态**：权限、菜单、路由、主题、标签页等状态必须有明确来源、生命周期和失效策略
8. **长期正确**：不保留旧系统兼容、灰度开关、fallback 与 deprecated 逻辑，避免基础设施复杂度累积

---

## 2. 总体技术要求（固定工程基线）

本系统为全新前端底座，不兼容 legacy/旧代码/旧数据，不保留灰度、fallback、deprecated 逻辑。技术栈不再作为“可替换建议”，而是作为架构边界和验收基线。

| 维度 | 要求 |
| --- | --- |
| 运行环境 | Node.js 使用 React Router 最新稳定版要求的 LTS 基线；当前基线为 Node.js 22.22.0+ |
| 框架 | React 最新稳定版，当前基线为 React 19.2.7+；统一使用函数组件、Hooks 与并发渲染友好的写法 |
| 构建 | Vite 最新稳定版；支持开发热更新、生产构建、多环境变量、按路由拆包和构建产物分析 |
| 语言 | 全量 TypeScript，启用严格模式；核心实体、接口响应、路由元信息、权限模型必须有显式类型 |
| UI 组件 | shadcn/ui 作为基础组件体系，组件代码本地化管理；底层遵循 Radix UI 可访问性语义 |
| 图标 | 默认使用 `lucide-react`；业务自定义 SVG 必须封装为 React 组件，不允许散落内联 SVG |
| 样式 | shadcn 主题使用 CSS Variables；布局与业务样式使用 less/less modules；主题令牌统一映射到 CSS 变量，禁止在业务样式中硬编码语义颜色 |
| 状态管理 | Redux Toolkit 作为客户端全局状态入口；仅管理登录态、权限态、主题、布局、多标签页等客户端状态 |
| 数据请求 | 基于 axios 封装统一 API Client，集中处理 Token 注入、401/403、错误归一化、超时、重试、取消请求和下载上传 |
| 路由 | React Router 最新稳定版，当前基线为 React Router 8.2.0+；使用集中路由注册表、嵌套路由、懒加载和统一守卫 |
| 表单 | react-hook-form + zod；表单结构、校验规则、错误文案按模块集中维护 |
| 测试 | Vitest + React Testing Library 覆盖基础设施和关键组件；权限、路由、请求、状态流测试不低于 P1 |

### 2.1 架构边界

项目必须按“基础设施层 → 领域模块层 → 页面编排层 → 组件表现层”分层，禁止跨层直接依赖。

| 层级 | 职责 | 禁止事项 |
| --- | --- | --- |
| 基础设施层 | store、router、request、theme、i18n、permission、config 等框架能力 | 禁止引用具体业务页面和业务组件 |
| 领域模块层 | 用户、角色、权限、菜单等领域模型、API、slice、选择器、领域工具 | 禁止直接操作 DOM、路由实例和全局样式 |
| 页面编排层 | 组合领域能力与 UI 组件，承载页面级交互和加载状态 | 禁止沉淀可复用业务规则，禁止巨型页面组件 |
| 组件表现层 | shadcn 基础组件封装、业务通用展示组件、表单控件、表格控件 | 禁止直接请求接口，禁止读取环境变量或路由全局状态 |

### 2.2 数据流与状态流

- 服务端数据只能通过领域模块的 API 函数获取，API 函数内部统一调用 axios API Client，禁止页面组件直接调用 axios 实例或原生 `fetch`。
- 客户端状态按域拆分为 `auth`、`appSettings`、`layout`、`permission`、`tabs` 等 slice，禁止隐式模块级变量保存状态。
- 派生数据通过 selector 计算，禁止在多个组件中重复拼装权限、菜单、面包屑等结构。
- 表单临时状态优先由 react-hook-form 管理；需要跨页面保留时显式进入 Redux 或 sessionStorage，并标明生命周期。
- 主题、语言、布局偏好等持久化状态必须经统一持久化适配器读写，禁止散落访问 localStorage。
- 列表数据、详情数据等服务端响应默认停留在页面局部状态或领域 hooks 内；只有登录用户、权限、菜单、布局偏好等跨页面客户端状态可以进入 Redux。

### 2.3 路由、菜单与权限流

- 系统只保留一种长期架构：后端返回菜单、权限码和 `routeKey/componentKey`，前端通过白名单路由注册表映射真实组件。
- 后端不得返回任意前端组件路径；前端不得根据后端字符串拼接动态 import 路径。
- 路由注册表是页面组件的唯一入口，包含 `routeKey`、`path`、`component`、`meta`、`loader`、`permissionCode` 等信息。
- 菜单树用于导航展示，权限码用于访问控制，路由注册表用于组件映射，三者职责独立但通过稳定 key 关联。
- 前端权限只负责体验优化和路由拦截，最终鉴权以后端结果为准。

### 2.4 错误处理与可观测性

- API 错误统一归一化为稳定错误结构，页面只消费业务错误码和可展示文案。
- 401/403/404/500 分别走统一处理链路，不允许各页面自行重复实现。
- 错误边界覆盖布局层和模块页面层，避免单个模块异常导致整站白屏。
- 关键基础设施需保留可测试的纯函数边界，例如权限判定、菜单归一化、路由过滤、主题变量生成。

### 2.5 API Client 分层设计

axios 请求层属于基础设施层，只负责 HTTP 管道和协议适配，不承载业务规则。

| 模块 | 职责 |
| --- | --- |
| `httpClient` | 创建 axios 实例，配置 `baseURL`、`timeout`、请求头、请求/响应拦截器 |
| `requestInterceptor` | 注入 Token、语言、租户、请求追踪 ID 等通用请求上下文 |
| `responseInterceptor` | 处理响应拆包、401/403、错误归一化、文件下载响应、业务错误码分发 |
| `apiError` | 定义统一错误结构，屏蔽 axios 原始错误差异 |
| `apiModules` | 按领域封装 API 函数，例如 `userApi.list`、`roleApi.create`、`menuApi.tree` |
| `domainHooks` | 可选封装页面级请求状态，例如 `useUserList`，内部调用领域 API 函数 |

请求层约束：

- 页面组件不得直接导入 axios 实例，只能调用领域 API 函数或领域 hooks。
- 请求拦截器只能处理技术上下文，不写业务判断。
- 响应拦截器只能做协议归一化和全局认证处理，不直接操作具体页面状态。
- 401 只派发会话失效 action，由统一监听器处理清理状态和跳转登录。
- 业务错误以稳定错误码向上抛出，由页面或领域 hooks 决定展示方式。
- 文件上传、文件下载、Blob、流式响应等特殊请求通过专用 API 函数封装，禁止在页面中临时拼 axios 配置。

---

## 3. 界面设计规范

> 本章定义模板的整体视觉语言、布局骨架与各界面区域的规格，作为 UI 实现与还原的统一基线。实现时可采用本项目的具体方案作为参考蓝本。

### 3.1 设计语言与视觉基调

| 维度 | 规范 |
| --- | --- |
| 色彩体系 | 以 Design Token 统一管理语义色：`background`(背景) / `background-paper`(卡片纸面) / `primary`(主色) / `success` / `warning` / `error` / `info` / `text-primary` / `text-secondary` / `text-disabled` / `border` 等；同一语义令牌在明暗两套主题下分别映射 |
| 明暗模式 | 支持浅色/深色双主题，所有色彩通过语义令牌切换，禁止硬编码语义颜色值 |
| 字体 | 提供可切换字体族（如 Inter / Open Sans），字号由令牌驱动；正文、标题、副标题、说明文字、标题层级（h1–h6）有明确规范 |
| 圆角与阴影 | 统一圆角令牌（如卡片圆角、按钮圆角）与阴影层级（卡片阴影、悬浮阴影、抽屉阴影） |
| 间距与栅格 | 统一间距阶梯（4/8/12/16/24/32…），通过 CSS 变量和 less mixin 复用，页面采用响应式栅格布局 |
| 毛玻璃质感 | 顶栏、抽屉等悬浮层采用半透明背景 + 背景模糊（backdrop-blur） |
| 背景层次 | 登录页、设置/通知抽屉等可使用低对比度背景纹理或渐变层次，但不得依赖离散光斑/装饰球作为主要视觉表达 |
| 图标 | 统一使用 `lucide-react`；本地 SVG 需封装为可复用图标组件，尺寸统一为 16/18/20/24 |
| 动效 | 布局/主题切换有平滑过渡，过渡时长与缓动由 Design Token 管理；按钮悬浮、抽屉展开、列表项动画自然，避免无意义循环动效 |

### 3.2 整体布局骨架

模板须提供**响应式、可扩展**的整体框架，由「侧边导航 + 顶部工具栏 + 内容区」组成。P0 默认实现垂直布局与移动端抽屉布局；迷你布局、多标签页和水平布局按优先级作为可配置扩展。

**形态 A — 垂直布局（侧边栏，默认）**
```
┌────────┬───────────────────────────────┐
│        │  顶部工具栏（sticky + 毛玻璃） │
│ 侧边   ├───────────────────────────────┤
│ 导航   │  多标签页（可选）              │
│ (固定) ├───────────────────────────────┤
│        │                               │
│        │       内容区（居中、卡片化）   │
│        │                               │
└────────┴───────────────────────────────┘
```

**形态 B — 迷你布局（P0：侧边栏折叠态）**
侧边栏收窄为仅图标宽度（如 64px），仅展示图标，鼠标悬浮显示文字提示；其余结构同形态 A。

**形态 C — 水平布局（顶部导航，P1）**
```
┌───────────────────────────────────────┐
│  Logo       顶部工具栏（毛玻璃）       │
├───────────────────────────────────────┤
│  横向一级导航（可下拉展开二级）        │
├───────────────────────────────────────┤
│  多标签页（可选）                      │
├───────────────────────────────────────┤
│       内容区（居中、卡片化）           │
└───────────────────────────────────────┘
```

**形态 D — 移动端布局（P0，≤ md 断点）**
- 侧边栏收起为抽屉，由顶部菜单按钮唤出
- 顶部工具栏 sticky，工具按钮保留核心项
- 内容区单列、间距收紧

#### 通用要求
- PC 布局共享同一套内容区与工具栏组件，仅导航位置不同
- 侧栏宽度变化、内容区 padding 变化须有过渡动画
- 断点由 less 变量集中定义，移动端（< md）自动切换为形态 D

### 3.3 侧边导航栏（垂直/迷你形态）

| 区域 | 规格 |
| --- | --- |
| 容器 | 固定定位、全高、背景与主背景区分、右侧虚线边框分隔 |
| Logo 区 | 顶部高度与工具栏对齐，展示 Logo 图标 + 应用名；迷你模式下应用名折叠隐藏（透明度+宽度过渡） |
| 折叠按钮 | 位于 Logo 区右侧，跨边线悬浮（半圆外凸），点击在「垂直 ↔ 迷你」间切换 |
| 菜单列表 | 可滚动区域（自定义滚动条）；按分组（group）分区展示，组标题为小字说明 |
| 菜单项 | 图标 + 文字；选中态高亮（主色背景/文字）；悬浮态反馈；支持徽标（Badge 角标，如未读数 +12、New 标签） |
| 多级菜单 | 数据模型支持多级树；导航 UI 默认展示 1–3 级，超过 3 级通过分组、搜索或下钻页面承载，避免深层嵌套影响可用性 |
| 特殊态 | 禁用项（灰显不可点）、外链项（右侧外链图标）、隐藏项（不在导航显示但路由可达） |
| 权限过滤 | 无权限菜单项不渲染 |

### 3.4 顶部工具栏（Header）

| 要求 | 规格 |
| --- | --- |
| 定位 | sticky 置顶，`z-index` 高于内容；半透明背景 + 毛玻璃模糊 |
| 高度 | 由令牌统一（如 `--layout-header-height`） |
| 左侧 | 布局占位（水平布局放 Logo，移动端放菜单触发）+ 面包屑（md 以上显示） |
| 右侧工具组 | 从左到右典型排列：全局搜索 → 语言切换 → 外链入口（GitHub/Discord 等，可选）→ 通知铃铛（带角标）→ 主题设置齿轮 → 账户头像下拉 |
| 按钮风格 | 统一为圆形 ghost 图标按钮，hover 有背景反馈；设置齿轮可带慢速旋转动效 |
| 响应式 | 小屏隐藏次要项，保留搜索/通知/账户 |

### 3.5 多标签页（Multi-Tabs）

| 要求 | 规格 |
| --- | --- |
| 位置 | 紧贴顶部工具栏下方 |
| 样式 | 卡片式标签（card 类型、紧凑尺寸），标签间留有间距 |
| 交互 | 单击切换页面、标签可关闭（×）、支持「关闭其他/关闭全部/刷新/固定当前」右键或下拉菜单 |
| 拖拽 | 标签可拖拽排序 |
| 滚动 | 标签过多时横向滚动，支持鼠标滚轮转横向滚动；激活标签自动滚入可视区 |
| 状态保持 | 已打开标签的查询条件、分页、滚动位置等状态必须显式持久化；React 页面组件不依赖隐式 keep-alive |
| 角标 | 标题可显示对应菜单图标 |

### 3.6 面包屑导航

- 基于当前路由层级**自动生成**，无需手写
- 末级为当前页面（不可点击、加粗），前序层级为可点击链接
- 含子级的层级节点显示下拉箭头，可切换同级页面
- 层级过深时，中间层级折叠为省略号（…），点击展开完整路径
- 移动端隐藏

### 3.7 内容区（Main）

- 顶部、左右留白，响应式内边距（小屏收紧、大屏放宽）
- **居中限宽**：默认最大宽度居中（如 `xl:max-w-screen-xl`），可在设置中开启"拉伸铺满"
- 切换布局/主题时最大宽度有过渡动画
- 懒加载（Suspense + 骨架/线性 Loading）、滚动位置恢复
- **卡片化**：业务内容默认包裹在 shadcn `Card` 内（含 Header / Content / Action 槽位）
- 网格布局：统计卡片常用 1/2/4 列响应式网格，具体断点和间距由 less 变量控制

### 3.8 全局搜索（命令面板）

- 快捷键 `⌘K / Ctrl+K` 唤起命令面板（cmdk 风格）
- 输入框 + 结果列表，对全站导航项做模糊搜索
- **命中高亮**：匹配文字主色高亮
- 结果项展示：菜单标题（翻译后）+ 路径（等宽小字）
- 选中回车跳转、ESC 关闭
- 底部展示快捷键提示（↑↓ 导航 / ↵ 选择 / ESC 关闭）
- 触发按钮：顶栏搜索按钮，常显示快捷键徽标提示

### 3.9 主题设置面板

- 右侧**抽屉（Sheet）**形式，不遮挡主交互（非模态）
- 抽屉背景：毛玻璃 + 低对比度背景层次，避免装饰元素干扰操作区
- 分区清晰，每区有小标题：
  - **主题模式**：浅色 / 深色，两个大卡片可点切换，选中态主色高亮
  - **布局**：垂直 / 迷你 / 水平，用**可视化缩略图卡片**直观展示布局形态；下方"内容拉伸"开关
  - **主题色预设**：一组竖条色块，点击切换主色，选中色块加宽 + 勾选标记
  - **字体**：字体族卡片选择（展示 Aa 样例）+ 字号滑块（12–20px）
  - **页面配置**：面包屑开关等
- **实时预览**：所有设置即时生效
- 底部：全屏切换按钮

### 3.10 通知中心

- 顶栏铃铛 + 未读角标（圆形红色数字），点击打开**右侧抽屉**
- 抽屉头部：标题 + 全部已读按钮
- **分类 Tab**：全部 / 收件箱 / 团队 等，每 Tab 带数量徽标
- 通知列表滚动，每条通知为**卡片**，按类型变体渲染：
  - 基础：头像 + 操作描述（"A 提到了你 in B"）+ 时间 + 部门
  - 文件类：文件图标 + 文件名 + 大小 + 下载按钮
  - 会议类：日期徽标 + 会议标题 + 时间段 + 参会人头像组
  - 任务类：任务标题 + 截止日 + 标签 + 指派人头像组
  - 含操作类：Accept / Decline 按钮组
  - 可回复类：内联回复输入框
- 底部：全部归档 / 全部已读

### 3.11 账户菜单

- 头像触发下拉
- 顶部展示头像 + 用户名 + 邮箱
- 菜单项：外部文档链接、个人资料、账户设置
- 退出登录项以**警告色**突出显示
- 退出后清理状态并跳转登录页

### 3.12 登录页

- **双栏布局**（大屏两列，小屏单列）：
  - 左栏：Logo + 应用名（顶部）+ 居中登录卡片（最大宽度限制，如 max-w-xs）
  - 右栏：整幅背景图，深色模式下自动降亮度 + 灰度处理
- 右上角悬浮：语言切换 + 主题设置按钮
- 登录卡片内集成多种形式（账号密码 / 手机 / 扫码 / 注册 / 重置），同区域内平滑切换，不跳页
- 已登录用户访问登录页自动跳转默认首页

### 3.13 数据表格与表单规范

- **列表页**：卡片包裹，卡片头部含标题 + 「新增」按钮；表格支持分页、横向滚动、紧凑行高；状态列用彩色徽标（Badge），操作列为图标按钮组（编辑/删除等）
- **表单**：弹窗（Modal）或抽屉（Drawer）承载新增/编辑表单；字段校验实时反馈；错误信息行内显示
- **详情页**：独立路由页面（如 `/user/:id`），卡片分区展示
- **空数据**：表格、列表、搜索无结果时展示友好的空状态插画/文案
- 数字大额自动格式化（千分位）、金额带货币符号、百分比带涨跌色

### 3.14 反馈与状态

| 场景 | 规范 |
| --- | --- |
| 消息通知 | 轻量 Toast（sonner 风格），成功/失败/警告/信息四类，支持关闭按钮，右上角堆叠 |
| 加载态 | 线性 Loading（路由/区域加载）、按钮内 spinner、骨架屏 |
| 确认操作 | 危险操作（删除）用确认弹窗二次确认 |
| 错误页 | 403 / 404 / 500 三种，居中插画 + 文案 + 返回首页按钮，统一错误布局 |
| 全局异常 | 错误边界兜底，避免白屏 |

### 3.15 响应式与移动端适配

- 桌面优先，断点降级适配平板与手机
- 移动端：侧栏转抽屉、隐藏面包屑/多标签页次要项、内容单列、表格横向滚动
- 关键交互（登录、搜索、通知）在移动端可达
- 触控目标尺寸满足最小点击区域要求

---

## 4. 功能需求

> 每个模块统一按 **功能概述 → 功能点清单 → 业务规则 → 验收标准** 组织。界面规格见第 3 章。

### 4.1 注册模块

#### 功能概述
支持用户自助注册账号。后台系统注册能力按场景可选（部分系统仅由管理员创建账号），故整体为 P1。

#### 功能点清单
| 编号 | 功能点 | 优先级 | 描述 |
| --- | --- | --- | --- |
| REG-001 | 注册表单 | P1 | 用户名、密码、确认密码、邮箱/手机号、用户协议勾选 |
| REG-002 | 表单校验 | P1 | 必填、格式、密码强度、两次密码一致性实时校验 |
| REG-003 | 验证码 | P2 | 图形/短信/邮箱验证码，防机器注册 |
| REG-004 | 注册提交 | P1 | 调用注册接口，处理成功/失败、重复账号 |
| REG-005 | 注册成功引导 | P1 | 成功后提示并引导至登录或自动登录 |
| REG-006 | 注册开关 | P1 | 配置开启/关闭自助注册入口 |

#### 业务规则
- 用户名/邮箱/手机号全局唯一，重复时明确提示
- 密码满足最小强度；前端不持久化密码、不写日志，传输安全依赖 HTTPS 和后端认证策略
- 须勾选用户协议方可提交

#### 验收标准
- ✅ 合法输入注册成功；非法输入实时拦截并提示；重复账号被拒

### 4.2 登录模块

#### 功能概述
提供安全、完整的登录认证体系，是整个系统的入口。

#### 功能点清单
| 编号 | 功能点 | 优先级 | 描述 |
| --- | --- | --- | --- |
| LOG-001 | 账号密码登录 | P0 | 用户名/邮箱/手机号 + 密码 |
| LOG-002 | 登录态管理 | P0 | Token 生命周期、请求自动注入、过期检测、刷新和失效处理 |
| LOG-003 | 路由守卫 | P0 | 未登录访问受保护资源跳转登录；已登录访问登录页跳首页 |
| LOG-004 | 登出 | P0 | 清理状态与凭证，跳转登录页 |
| LOG-005 | 记住我 | P1 | 延长本地登录态保留时长 |
| LOG-006 | 忘记/重置密码 | P1 | 邮箱/手机验证重置密码完整流程 |
| LOG-007 | 登录安全策略 | P1 | 错误次数触发锁定/验证码；登录日志 |
| LOG-008 | 手机/短信登录 | P2 | 手机号 + 短信验证码 |
| LOG-009 | 扫码登录 | P2 | 移动端扫码确认（二维码轮询） |
| LOG-010 | 第三方登录 | P2 | OAuth（GitHub/微信/Google 等） |
| LOG-011 | 多形式切换 | P2 | 密码/手机/扫码/注册/重置在同页平滑切换 |

#### 业务规则
- Token 存储策略由安全配置明确指定；默认推荐内存访问令牌 + 可控刷新机制，禁止在业务组件中直接读写 Token
- 收到 401 等鉴权失效响应时，自动清理登录态并引导重登
- 登录失败信息脱敏（不区分"账号不存在"与"密码错误"）
- “记住我”只能影响刷新令牌或会话有效期，不允许把密码、明文凭证或敏感用户信息写入本地持久化
- 登录、登出、Token 刷新、改密、角色权限变更后，必须统一刷新 auth、permission、menu、route 与页面请求状态

#### 验收标准
- ✅ 正确凭证登录进入系统；错误凭证拦截提示
- ✅ 未登录访问受保护路由重定向登录页，登录后回跳
- ✅ Token 过期正确识别并触发重登；登出后受保护页面均不可访问
- ✅ 改密、登出和权限变更后，旧会话与旧权限缓存不会继续生效

### 4.3 用户管理

#### 功能概述
对系统用户的全生命周期管理。

#### 功能点清单
| 编号 | 功能点 | 优先级 | 描述 |
| --- | --- | --- | --- |
| USR-001 | 用户列表 | P0 | 卡片 + 表格分页，支持按用户名/姓名/状态搜索筛选 |
| USR-002 | 新增用户 | P0 | 弹窗/抽屉表单，录基本信息、初始密码、分配角色 |
| USR-003 | 编辑用户 | P0 | 修改用户信息与状态 |
| USR-004 | 删除用户 | P0 | 单个/批量删除，支持软删除（可恢复） |
| USR-005 | 启用/禁用 | P0 | 切换账号状态，禁用后无法登录 |
| USR-006 | 重置密码 | P1 | 管理员重置指定用户密码 |
| USR-007 | 分配角色 | P0 | 为用户分配一个或多个角色 |
| USR-008 | 用户详情 | P1 | 独立详情页，查看信息/角色/权限/操作日志 |
| USR-009 | 个人中心 | P0 | 当前用户改资料、改自己的密码 |
| USR-010 | 导入/导出 | P2 | 批量导入、导出列表 |

#### 业务规则
- 用户与角色多对多，实际权限为各角色权限并集
- 禁止删除/禁用当前登录用户自身
- 至少预置一个超级管理员，不可删除

#### 验收标准
- ✅ 列表分页/搜索/筛选正确；增删改启停生效并实时刷新
- ✅ 分配角色后，该用户可访问菜单与权限随之变化
- ✅ 个人中心改资料/改密可用，改密后需重新登录

### 4.4 角色管理

#### 功能概述
作为权限集合载体，通过给用户分配角色批量授权。

#### 功能点清单
| 编号 | 功能点 | 优先级 | 描述 |
| --- | --- | --- | --- |
| ROLE-001 | 角色列表 | P0 | 名称、编码、状态、描述、成员数 |
| ROLE-002 | 新增角色 | P0 | 名称、编码、描述、默认状态 |
| ROLE-003 | 编辑角色 | P0 | 修改角色信息 |
| ROLE-004 | 删除角色 | P0 | 删除（有用户关联时提示或禁止） |
| ROLE-005 | 启用/禁用 | P0 | 切换角色状态 |
| ROLE-006 | 权限分配 | P0 | 树形勾选分配菜单/按钮/数据权限 |
| ROLE-007 | 成员查看 | P1 | 查看角色下用户列表 |

#### 业务规则
- 角色编码全局唯一
- 删除前校验关联用户，有关联则阻止或提示解绑
- 权限变更后相关在线用户权限可刷新

#### 验收标准
- ✅ 增删改查启停生效
- ✅ 权限分配树形勾选（菜单+按钮）保存后更新
- ✅ 删除含关联用户的角色被拦截提示

### 4.5 权限管理

#### 功能概述
定义并维护权限资源与权限标识，提供多层级访问控制。

#### 功能点清单
| 编号 | 功能点 | 优先级 | 描述 |
| --- | --- | --- | --- |
| PERM-001 | 权限模型 | P0 | 菜单权限、按钮权限，可选数据权限 |
| PERM-002 | 权限标识 | P0 | 统一权限码（`模块:操作`），前后端共用 |
| PERM-003 | 权限树维护 | P1 | 可视化维护权限资源层级 |
| PERM-004 | 路由过滤 | P0 | 根据权限码和路由注册表生成当前用户可访问路由 |
| PERM-005 | 菜单过滤 | P0 | 导航菜单按当前用户权限过滤 |
| PERM-006 | 路由级控制 | P0 | 访问无权限路由跳转 403 |
| PERM-007 | 组件级控制 | P1 | 权限包裹组件，无权限渲染兜底或不渲染 |
| PERM-008 | 函数级控制 | P1 | `check/checkAny/checkAll` 工具方法 |
| PERM-009 | 数据权限 | P2 | 行级/字段级数据范围控制 |
| PERM-010 | 权限缓存刷新 | P1 | 权限缓存，支持刷新或变更后失效 |

#### 业务规则
- 权限判定遵循"角色并集"原则
- 超级管理员默认拥有全部权限，可跳过细粒度校验
- **前端权限控制仅为体验优化，所有权限必须在后端再次校验**
- 权限、菜单、路由三类数据独立建模，通过稳定 key 关联，不允许相互混用结构
- 当前用户权限刷新后，必须统一失效菜单、路由、按钮可见性和相关页面请求状态

#### 验收标准
- ✅ 不同角色登录后可见菜单与可访问页面不同
- ✅ 直接访问无权限路由被拦截展示 403
- ✅ 按钮按权限显隐；组件级与函数级鉴权 API 可用

### 4.6 菜单管理

#### 功能概述
管理导航菜单，采用“后端菜单数据 + 前端路由注册表”的单一架构，驱动导航渲染与路由访问控制。

#### 功能点清单
| 编号 | 功能点 | 优先级 | 描述 |
| --- | --- | --- | --- |
| MENU-001 | 菜单来源 | P0 | 后端返回菜单树、权限码和 `routeKey/componentKey`，前端负责安全映射 |
| MENU-002 | 菜单类型 | P0 | 目录/菜单/按钮 |
| MENU-003 | 树形结构 | P0 | 支持多级树形数据，导航默认展示 1–3 级 |
| MENU-004 | 菜单属性 | P0 | 名称、图标、路由、`routeKey/componentKey`、权限码、排序、显隐、缓存策略 |
| MENU-005 | 增删改查 | P0 | 节点新增/编辑/删除/查看 |
| MENU-006 | 路由关联 | P0 | 菜单通过稳定 key 与前端路由注册表正确映射 |
| MENU-007 | 多级渲染 | P1 | 多级嵌套导航正确展开/收起 |
| MENU-008 | 外链/Iframe | P1 | 外链（新窗口）与内嵌 iframe |
| MENU-009 | 页面状态策略 | P1 | 可配置查询条件、分页、滚动位置等显式状态保留策略 |
| MENU-010 | 排序与显隐 | P1 | 拖拽/数值排序、按需隐藏（路由仍可达） |
| MENU-011 | 菜单状态 | P1 | 禁用态、徽标/标签展示 |

#### 业务规则
- 目录仅作容器，不对应页面
- 删除含子节点菜单须级联或阻止
- 后端只返回当前用户可见菜单；前端仍需基于权限码二次过滤
- 后端返回的 `routeKey/componentKey` 必须命中前端白名单注册表，未命中时进入统一异常页并记录错误
- 禁止后端返回任意前端组件路径，禁止前端拼接字符串动态导入组件
- 隐藏菜单不在导航展示，但必须仍受路由权限控制

#### 验收标准
- ✅ 后端菜单数据与前端路由注册表能正确映射并渲染
- ✅ 多级菜单正常展开/收起并高亮当前路由
- ✅ 外链新窗口打开、Iframe 正确内嵌
- ✅ 查询条件、分页、滚动位置等按配置显式保留；隐藏菜单不显示但路由可达且受权限控制

### 4.7 布局与主题（可配置项）

> 界面骨架与视觉规格见第 3 章。本节聚焦"用户可配置"的布局/主题功能点。

#### 功能点清单
| 编号 | 功能点 | 优先级 | 描述 |
| --- | --- | --- | --- |
| LAY-001 | 响应式适配 | P0 | 桌面/平板/移动端自适应（见 3.2/3.17） |
| LAY-002 | 多种导航布局 | P1 | 侧边（垂直）/顶部（水平）/迷你可切换（见 3.2） |
| LAY-003 | 侧边栏折叠 | P0 | 折叠/展开并记忆（见 3.3） |
| LAY-004 | 多标签页 | P1 | 页签开关/关闭/关闭其他/刷新/拖拽排序（见 3.5） |
| LAY-005 | 面包屑 | P1 | 自动生成、可开关（见 3.6） |
| LAY-006 | 全局搜索 | P2 | ⌘K 命令面板（见 3.8） |
| LAY-007 | 通知中心 | P2 | 站内通知入口与角标（见 3.10） |
| LAY-008 | 用户菜单 | P0 | 头像下拉（见 3.11） |
| LAY-009 | 全屏切换 | P2 | 一键全屏（见 3.9） |
| THM-001 | 明暗模式 | P1 | 浅色/深色切换，可跟随系统（见 3.1） |
| THM-002 | 主题色 | P1 | 预设多套主色 + 自定义（见 3.9） |
| THM-003 | 设计令牌 | P1 | 颜色/字体/圆角/阴影/间距令牌化（见 3.1） |
| THM-004 | 字体设置 | P2 | 字体族与字号配置（见 3.9） |
| THM-005 | 内容宽度 | P2 | 流式/定宽切换（见 3.7） |
| THM-006 | 设置持久化 | P1 | 布局/主题偏好本地或云端持久化 |
| THM-007 | 实时预览 | P1 | 设置面板实时生效（见 3.9） |

#### 业务规则
- 主题与布局设置本地持久化，刷新后保留
- 切换明暗模式时图表/富文本等第三方组件同步适配

#### 验收标准
- ✅ 各终端布局不错位、关键操作可达
- ✅ 多种布局/主题平滑切换并实时生效，刷新后保留

### 4.8 国际化（i18n）

#### 功能点清单
| 编号 | 功能点 | 优先级 | 描述 |
| --- | --- | --- | --- |
| I18N-001 | 多语言框架 | P1 | 成熟 i18n 方案接入 |
| I18N-002 | 语言包管理 | P1 | 按模块/命名空间拆分 |
| I18N-003 | 语言切换 | P1 | 运行时切换无需刷新，全站即时更新 |
| I18N-004 | 语言检测 | P1 | 自动检测浏览器语言，支持默认语言 |
| I18N-005 | 语言持久化 | P1 | 记住用户语言偏好 |
| I18N-006 | 本地化格式 | P2 | 日期/时间/数字/货币按区域格式 |
| I18N-007 | 内置语言 | P1 | 至少内置中文、英文两套完整语言包 |
| I18N-008 | RTL 支持 | P2 | 从右到左布局（可选） |

#### 业务规则
- 所有面向用户文案经 i18n，禁止硬编码
- 后端返回的字典/枚举文本支持按当前语言本地化

#### 验收标准
- ✅ 切换语言后全站文案（含动态校验提示）即时变更
- ✅ 刷新后语言偏好保留；新增语言仅需补充语言包

### 4.9 前端工程化

#### 功能点清单
| 编号 | 功能点 | 优先级 | 描述 |
| --- | --- | --- | --- |
| ENG-001 | 项目脚手架 | P0 | 标准化目录结构与分层，符合 2.1 架构边界 |
| ENG-002 | 组件化体系 | P0 | shadcn 基础组件本地化封装，业务组件按职责复用 |
| ENG-003 | 状态管理 | P0 | Redux Toolkit store 按域划分，selector 统一导出 |
| ENG-004 | 网络请求封装 | P0 | axios API Client 统一处理 Token 注入、错误归一化、超时、重试、取消请求与认证失效 |
| ENG-005 | 路由与守卫 | P0 | React Router 集中注册表 + 统一守卫 + 懒加载边界 |
| ENG-006 | 类型安全 | P0 | 全量 TypeScript，类型定义完整 |
| ENG-007 | 代码规范 | P0 | ESLint + TypeScript 检查 + Stylelint；格式化脚本显式触发，不做无关全量格式化 |
| ENG-008 | 提交规范 | P1 | commitlint + Git Hooks |
| ENG-009 | Mock 方案 | P1 | 前端独立 Mock，经环境配置切换，业务代码不感知 |
| ENG-010 | 多环境配置 | P0 | 环境变量 schema 校验，管理 dev/test/prod |
| ENG-011 | 全局错误处理 | P0 | 错误边界、404/500 兜底、异常上报 |
| ENG-012 | 构建优化 | P1 | 懒加载、代码分割、Tree-shaking、打包分析 |
| ENG-013 | 测试 | P1 | 权限、路由、请求、store、核心组件必须有单测或集成测试 |
| ENG-014 | CI/CD | P2 | typecheck → lint → test → build → 部署流水线 |
| ENG-015 | 文档 | P1 | README、组件用法、二次开发指南、CHANGELOG |

#### 业务规则
- 环境变量通过 schema 校验；缺失必填项应在启动或构建阶段失败，避免运行时隐式 fallback
- Mock 与真实接口切换由配置驱动，不侵入业务代码
- 全局配置集中管理，禁止散落读取环境变量
- Redux store、axios API Client、路由注册表、权限工具必须各自独立导出，避免循环依赖

#### 验收标准
- ✅ 一键安装 → 一键启动开发；一键构建产出生产包并通过类型检查
- ✅ Lint 与提交规范在链路中生效
- ✅ 切换环境变量后 API 地址、路由模式等正确变化
- ✅ 权限判定、菜单归一化、路由过滤、axios 错误归一化均有可执行测试

---

## 5. 非功能需求

| 维度 | 要求 |
| --- | --- |
| 性能 | 首屏 LCP 有明确基线并持续监控；路由切换无明显卡顿；大表格/长列表使用分页或虚拟滚动；按路由和重型依赖合理分包 |
| 安全 | 强制 HTTPS 场景假设；密码不进入本地存储、日志和持久化状态；服务端负责密码单向哈希存储；敏感字段脱敏；Token/Refresh Token 策略明确；cookie 模式必须防御 CSRF；防御 XSS/开放重定向；鉴权以后端为准 |
| 兼容性 | 主流现代浏览器（Chrome/Edge/Firefox/Safari）最近两个大版本；不要求 IE；桌面与移动端尺寸自适应 |
| 可维护性 | 目录与模块边界清晰；配置集中、命名规范；核心逻辑有简体中文说明；公共工具有文档；禁止巨型组件、隐式状态、重复权限判断和跨层耦合 |
| 可测试性 | 权限、路由、请求、状态、主题变量生成等基础设施必须可单测；页面级能力通过集成测试覆盖关键路径 |
| 无障碍 | 关键交互支持键盘；表单有 label，图标按钮有 aria-label；颜色对比度满足可读性 |

---

## 6. 交付物清单

| 类别 | 交付物 |
| --- | --- |
| 源代码 | 完整可运行的前端模板工程 |
| 配置 | 多环境配置示例、Mock 数据、默认菜单与权限数据 |
| 文档 | README、需求文档、界面设计规范、二次开发指南、组件说明、CHANGELOG |
| 脚本 | 开发、构建、预览、lint 等 scripts |
| 预置数据 | 超级管理员账号、默认角色与权限、默认系统菜单 |

---

## 7. 模块优先级速查表

| 模块 | P0（必备） | P1（推荐） | P2（可选） |
| --- | --- | --- | --- |
| 注册 | — | 注册表单/校验/提交/开关 | 验证码 |
| 登录 | 账密登录、登录态、路由守卫、登出 | 记住我、重置密码、安全策略 | 短信/扫码/第三方登录 |
| 用户管理 | 列表、增删改、启停、分角色、个人中心 | 详情、重置密码 | 导入导出 |
| 角色管理 | 列表、增删改、启停、权限分配 | 成员查看 | — |
| 权限管理 | 模型/标识、路由过滤、菜单过滤、路由级控制 | 权限树、组件/函数级控制、缓存刷新 | 数据权限 |
| 菜单管理 | 后端菜单来源、类型、树结构、属性、CRUD、路由注册表关联 | 多级渲染、外链/Iframe、显式页面状态策略、排序 | — |
| 布局与主题 | 响应式、侧边折叠、用户菜单 | 多布局、多标签页、面包屑、明暗模式、主题色、令牌、持久化 | 搜索、通知、全屏、字体、内容宽度 |
| 国际化 | — | 框架、语言包、切换、检测、持久化、内置中英 | 本地化格式、RTL |
| 前端工程化 | 脚手架、组件化、Redux Toolkit 客户端状态、axios 请求层、React Router 路由、类型、规范、多环境、错误处理 | 提交规范、Mock、构建优化、测试、文档 | CI/CD |

---

> **使用说明**：本文档为通用基线，落地时可根据实际项目裁剪（关闭 P2、按需取舍 P1），P0 项建议作为后台模板的最低交付标准。第 3 章「界面设计规范」可独立作为 UI 还原与走查依据。
